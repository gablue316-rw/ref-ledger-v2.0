services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: ref-ledger-cloudflared
    restart: unless-stopped
    command:
      - tunnel
      - --no-autoupdate
      - run
      - --token
      - ${CLOUDFLARE_TUNNEL_TOKEN}